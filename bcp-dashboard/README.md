# Merged into bcp-web

This app has been merged into **bcp-web**.

All new dashboard pages are at `/nd/*` in bcp-web (port 3002).

This folder is kept for reference only and is not run.

Navigate to [http://localhost:3002/nd](http://localhost:3002/nd) after configuring environment variables in bcp-web (see `SETUP_NEW_DASHBOARD.md` in bcp-web).
